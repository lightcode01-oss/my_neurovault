### Example environment variables for Hardhat deployments
# Do NOT commit real secrets

RPC_URL=
PRIVATE_KEY=
ARBITRUM_SEPOLIA_RPC=
ETHERSCAN_API_KEY=
ARBITRUM_API_KEY=

# Optional: network override
NETWORK=arbitrumSepolia
# NeuroVault Memory Network — Environment Variables Template
#
# Copy this file to .env.local and fill in the values for your deployment.
# NEVER commit secrets to git. Use GitHub Secrets for CI/CD.

# ==================== FRONTEND ====================

# Backend API
VITE_BACKEND_URL=http://localhost:8000

# IPFS Configuration
VITE_IPFS_API_URL=http://localhost:5001
VITE_IPFS_GATEWAY_URL=https://gateway.ipfs.io

# Web3 Storage (fallback for IPFS uploads)
VITE_WEB3_STORAGE_TOKEN=

# Smart Contract Module ID (set after deployment)
VITE_STYLUS_MODULE_ID=

# Monitoring & Errors
VITE_SENTRY_DSN=

# ==================== BACKEND ====================

# Server Configuration
BACKEND_HOST=localhost
BACKEND_PORT=8000
DEBUG=neurovault:*
LOG_LEVEL=info

# Database
DATABASE_URL=sqlite:///./test.db
SQLITE_DB_PATH=./test.db
DB_PATH=./backend/data/neurovault.sqlite3

# IPFS (backend)
IPFS_API_URL=http://localhost:5001
IPFS_GATEWAY_URL=https://gateway.ipfs.io
WEB3_STORAGE_TOKEN=

# Validator Configuration
VALIDATOR_KEY=                                 # TODO: Private key (keep secret!)
VALIDATOR_ENDPOINT=http://localhost:8001
VALIDATOR_MODE=simulate                        # or 'live' (requires VALIDATOR_KEY)
VALIDATE_SYNC=0                                # if 1, backend triggers validation synchronously on POST /memories

# Monitoring
SENTRY_DSN=

# ==================== SMART CONTRACT & DEPLOYMENT ====================

# Stylus WASM Deployment
STYLUS_NODE_ENDPOINT=https://sepolia-rollup.arbitrum.io/rpc
STYLUS_NODE_RPC=https://sepolia-rollup.arbitrum.io/rpc
RPC_URL=https://sepolia-rollup.arbitrum.io/rpc
STYLUS_NETWORK=arbitrumSepolia                 # or arbitrumOne for mainnet
STYLUS_MODULE_ID=                              # Deployed module ID (set after deployment)
DEPLOYER_KEY=                                  # TODO: Private key (keep secret!)

# Network RPCs (for multi-chain deployment)
ARBITRUM_SEPOLIA_RPC=https://sepolia-rollup.arbitrum.io/rpc
ARBITRUM_ONE_RPC=https://arb1.arbitrum.io/rpc
BASE_MAINNET_RPC=https://mainnet.base.org
BASE_GOERLI_RPC=https://goerli.base.org
OPTIMISM_MAINNET_RPC=https://mainnet.optimism.io
OPTIMISM_GOERLI_RPC=https://goerli.optimism.io
ZKSYNC_MAINNET_RPC=https://mainnet.era.zksync.io
ZKSYNC_TESTNET_RPC=https://testnet.era.zksync.dev

# ==================== CI/CD & RELEASE ====================

# GitHub
GH_TOKEN=                                      # TODO: GitHub token (keep secret!)

# WASM Artifact Verification
WASM_SHA256=                                   # SHA256 of published WASM artifact

# Release Configuration
RUN_E2E=0                                      # Set to 1 in CI to run E2E tests
DRY_RUN=1                                      # Set to 0 to perform actual deployment

# GPG Signing (for release artifacts)
GPG_KEY_ID=                                    # TODO: GPG key ID (keep secret!)

# ==================== INDEXER & BLOCK TRACKING ====================

# Block Indexing
LAST_INDEXED_BLOCK=0                           # Auto-updated by indexer
REINDEX_FROM_BLOCK=0                           # Force reindex from this block

# ==================== MONITORING & ALERTING ====================

# Prometheus Metrics (optional)
METRICS_PORT=9090
PROMETHEUS_ENABLED=0

# Sentry Error Tracking (optional)
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=0.1

# ==================== SECURITY ====================

# Rate Limiting (token bucket)
RATE_LIMIT_ENABLED=1
RATE_LIMIT_REQUESTS_PER_MINUTE=100

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ==================== DEVELOPMENT ====================

# Verbose logging
VERBOSE=0

# Test mode (uses mocks instead of real services)
TEST_MODE=0

# E2E Testing
E2E_ENDPOINT=http://localhost:8000            # Live endpoint for E2E tests

# Feature Flags
ENABLE_CACHE=1
ENABLE_INDEXER=1
ENABLE_VALIDATOR=1
VITE_IPFS_GATEWAY=https://dweb.link/ipfs
VITE_BLOCK_EXPLORER_BASE=https://sepolia.arbiscan.io
VITE_TARGET_CHAIN_ID=421614
