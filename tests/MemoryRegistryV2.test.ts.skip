// Skipped test (requires Hardhat). Kept for reference.

/*
This test was skipped automatically because the project was migrated to Stylus/WASM
and running Hardhat-based tests in the current environment causes import errors.
*/

export {};
