// Pinned module aliases removed — imports normalized to bare package names.
// If you previously relied on pinned import specifiers (e.g. "lib@1.2.3"),
// update your Vite alias configuration instead. This file intentionally
// contains no declarations now that code uses bare package imports.
